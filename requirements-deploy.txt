# Current FastAPI + LangGraph backend stack.
# The legacy Dash-only dependency set remains in the repository-root
# requirements.txt and must not be mixed into this deployment.
-r backend/requirements.txt
